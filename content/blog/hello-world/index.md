+++
draft = false
title = "Hello, world"
date = 2026-05-11
description = "placeholder first post"
tags = ["meta"]
+++

This is placeholder body text for the first blog post.

inline `code` renders via theme syntax highlighting. Block code test:

```c
int add(int x, int y){
    int sum = x + y;
    return sum;
}
```

A list:

- item one
- item two
- item three
